import React from 'react';
import {useSelector} from "react-redux";

const Cars = () => {
            const {cars} = useSelector(state => state)
    return (
        <div>

        </div>
    );
};

export default Cars;